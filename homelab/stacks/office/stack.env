# --- Required ---
POSTGRES_PASSWORD=

# --- Optional but recommended ---
POSTGRES_DB=
POSTGRES_USER=
NEXTCLOUD_HTTP_PORT=
NEXTCLOUD_TRUSTED_DOMAINS=localhost 127.0.0.1
PHP_MEMORY_LIMIT=
PHP_UPLOAD_LIMIT=
TZ=UTC

# If you want Redis auth, uncomment both lines here and in compose:
# REDIS_PASSWORD=choose-a-different-strong-password
# (and set REDIS_HOST_PASSWORD to this value under app:environment)
